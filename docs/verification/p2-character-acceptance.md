# P2-B 可绑定持矛角色验收

结论：Blender 5.2.1 LTS / macOS Apple Silicon 达到 L3。该角色是可编辑技术白模，不是电影级角色资产。

- 单一 Body 网格通过 Armature modifier 绑定 16 根骨骼；14 个变形组具有实际顶点权重。不是分段物体逐帧位移替代骨架。
- 四肢具有 IK 与极向目标，肘部有限制；移动右手控制器会改变下臂骨骼矩阵。FK 仍可直接通过 pose bones 编辑。
- 骨盆作为基础重心控制移动 8cm 时，支撑脚最大漂移 7.67mm，低于 1cm；Body 最低点高于地面约 5.48mm。
- 全程只有 `SpearHero_Spear` 一个长矛对象。约束影响在释放段为零，手—矛距离连续 30 帧超过 5cm，最大约 0.868m。
- 接回 90→91 帧位置跳变约 0.00000012m、旋转跳变 0°；骨长误差为 0。
- 以 1.8m 参数重新构建后，主要骨架缩放误差约 0.000000032m；释放/顶点/接回改为 50/63/78，无需重写配方。
- `.blend` 重开后 Armature、蒙皮 modifier、长矛约束和动画仍可编辑。抛矛顶点帧完成模型视觉检查。

交付目录：`/Users/wandl/workspaces/workspace-partme-ai/deliverables/codex-blender-p2-character-20260913-v3`。

限制：白模由一个网格中的断开代理块构成，权重为明确空间分组，不宣称自动权重或成片级肩髋变形；完整战斗表演、动作曲线清理、NLA、重定向和表情属于 P3。
