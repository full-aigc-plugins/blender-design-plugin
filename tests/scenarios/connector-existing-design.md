# Connector existing design

Given the user has opened Blender and explicitly clicked Start Connector, inspect the scene before
mutation. Revoke must immediately stop new commands and remove the private descriptor.
