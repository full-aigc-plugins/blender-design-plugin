# Managed new design

Given Blender is installed and no project is supplied, launch a foreground non-invasive session,
begin a milestone transaction, create the scene through registered commands, capture four fresh
views, commit the approved revision, and export under the approved output root.
