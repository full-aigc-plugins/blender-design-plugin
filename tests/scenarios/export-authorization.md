# Export authorization

Final export must bind the approved revision and snapshot, remain under the approved output root,
and carry action-bound authorization. Existing files require an explicit overwrite claim.
